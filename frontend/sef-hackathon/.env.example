# Leave empty when the API is served from the same host or when using the Vite proxy.
VITE_API_BASE_URL=
# For a separately hosted backend, use its origin, for example:
# VITE_API_BASE_URL=https://api.example.com
